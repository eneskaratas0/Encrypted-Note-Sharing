export function Footer() {
  return (
    <footer className="mx-auto w-full max-w-xl px-4 pb-8 text-center text-xs text-muted-foreground">
      Notes are end-to-end encrypted; the encryption key exists only in the share link and
      is never sent to the server.
    </footer>
  );
}
